import * as types from '../actions/action-types';

const initialState = {
  word: ''
}

export default function home(state = initialState, action = {}) {
  switch (action.type) {
    default:
      return state;
  }
}
