'use strict';
import React from 'react';
import Colors from './colors';

import {
    StyleSheet,
    PixelRatio
} from 'react-native';

var Styles = StyleSheet.create({
  pickerContainer: {
    marginLeft: 20,
    marginRight: 20,
    borderWidth: 1,
    borderColor: 'black',
  }
})

module.exports = Styles;
